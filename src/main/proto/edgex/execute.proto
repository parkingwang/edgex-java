syntax = "proto3";

package edgex;
option java_package = "edgex";
option java_multiple_files = true;

message Data {
    bytes frames = 1;
}

service Execute {
    rpc Execute (Data) returns (Data) {
    }
}

// ./gradoew clean build